# 排序

语法: db.集合名.find().sort(JSON数据)
说明: 键就是条件 值1就是升序-1就是降序


# limit与skip方法

语法: db.集合名.find().sort().skip(数字).limit(数字)
说明: skip跳过指定数量(可选) limit限制查询数量

$ skip计算公式
(当前页 - 1) * 每页显示条数


.count()   统计总数量